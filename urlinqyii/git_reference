git clone https://github.com/urlinq/alpha



//When you want to push your changes do this:
git add .
git commit -m 'your commit message here'

//Pull changes before you push ALWAYS
git pull origin master

//Push your changes
git push origin master

















//Set your fork URL
git remote set-url origin https://github.com/yourusername/alpha.git


//Sync your project with your current fork
git pull origin master

//Push your local project to your current fork
git push origin master

//Set master project url
git remote add upstream <path to upstream repo>

//Fetch upstream changes


//Merge those upstream changes with your fork
git merge upstream/master


//Push your fork to the master project
git push upstream


$ git fetch upstream
$ git merge upstream/master
$ git push origin master